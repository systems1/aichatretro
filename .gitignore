# User data (private — each person has their own exports, never commit these)
exports/
data/
*.pkl

# Environment / secrets
.env
.env.*

# Python
__pycache__/
*.pyc

# RAG runtime artifacts (local LLM models + serialized index — never commit)
models/
rag/index/

# Docker runtime
docker-compose.override.yml

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Claude Code working dir
.claude/
